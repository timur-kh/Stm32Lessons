{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build app",
            "group": "build",
            "type": "shell",
            "command": "cd build; Ninja"
        },
        {
            "label": "Build CMake for Ninja",
            "group": "build",
            "type": "shell",
            "command": "mkdir build; cd build; rm * -r; Cmake -G Ninja .."
        }
    ]
}